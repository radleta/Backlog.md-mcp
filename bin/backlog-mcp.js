#!/usr/bin/env node

/**
 * Backlog.md MCP Server CLI Entry Point
 * 
 * This is a simple wrapper that loads the compiled CLI from dist/
 * The actual implementation is in src/cli.ts
 */

import '../dist/cli.js';